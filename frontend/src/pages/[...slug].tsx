import React from 'react';
import App from '../App';

// Catch-all route - redirects everything to our App component
const CatchAllPage: React.FC = () => {
  return <App />;
};

export default CatchAllPage; 